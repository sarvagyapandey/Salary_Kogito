services:
  mongo:
    image: mongo:7
    container_name: payroll-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_DATABASE: payrollDB
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
